[pytest]
testpaths = tests
addopts = -ra --strict-markers --cov=app --cov-report=term-missing
markers =
    unit: Unit tests
    api: API tests
    integration: Integration tests
    slow: Slow tests
